import 'package:flutter/material.dart';

class AppColors {
  // Core Brand Colors (Premium Indigo)
  static const Color primary = Color(0xFF5E5CE6);
  static const Color accent = Color(0xFF7000FF);
  static const Color secondary = Color(0xFF344CB7);

  // Soft Light Mode Backgrounds
  static const Color background = Color(0xFFF8F9FA); // Sophisticated off-white
  static const Color surface = Color(0xFFFFFFFF); // Pure white
  static const Color glassSurface = Color(0xCCFFFFFF); // Frosted White (High opacity)

  // Typography (Slate/Charcoal focus)
  static const Color textPrimary = Color(0xFF1D1D1F); // Apple-style charcoal
  static const Color textSecondary = Color(0xFF6E6E73); // Muted slate gray

  // Status Colors
  static const Color breakingBadge = Color(0xFFE63946);
  static const Color success = Color(0xFF32D74B);
  static const Color error = Color(0xFFFF453A);

  // Standard Overlays & Shadows
  static const Color shadow = Color(0x1A000000); // 10% black soft shadow
  static const Color overlayDark = Colors.black26;
  static const Color overlayLight = Colors.white70;
}
